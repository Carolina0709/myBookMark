<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My BookMark</title>
    <link rel="stylesheet" href="/styles/index.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">

</head>
<body class="container bg-green-light">
    <header class="d-flex justify-content-between align-items-center mt-5">
        <h2 class="display-6 text-pink fst-italic fw-bold ">Edit book</h2>
        <div>
            <a href="<%= `/see/${book.id}` %>"class="btn btn-success btn-green">
                <i class="bi bi-arrow-return-left"></i>
            </a>
        </div>
    </header>
    <form action="<%= `/editBook/${book.id}` %>" method="POST" class="container w-100 pt-5" style="max-width: 750px;">
        <div class="form-floating mb-3">
            <input type="text" class="form-control input" id="title" name="title" value="<%= book.title %>">
            <label for="title" class="text-pink">
                <span class="bg-white rounded" style="padding: 2px;">Title</span>
            </label>
        </div>
        <div class="form-floating mb-3">
            <input type="text" class="form-control input" id="author" name="author" value="<%= book.author %>">
            <label for="author" class="text-pink">
                <span class="bg-white rounded" style="padding: 2px;">Author</span>
            </label>
        </div>
        <div class="form-floating mb-3">
            <input type="text" class="form-control input" id="isbn" name="isbn" value="<%= book.isbn %>">
            <label for="isbn" class="text-pink">
                <span class="bg-white rounded" style="padding: 2px;">ISBN</span>
            </label>
        </div>

        <div class="form-floating mb-3">
            <input type="text" class="form-control input" id="category" name="category" value="<%= book.category %>">
            <label for="category" class="text-pink">
                <span class="bg-white rounded" style="padding: 2px;">Category</span>
            </label>
        </div>
        
        <div class="form-floating mb-3">
            <input type="date" class="form-control input" id="date_read" name="date_read"  value="<%= new Date(book.date_read).toISOString().slice(0, 10) %>">
            <label for="date_read" class="text-pink">
                <span class="bg-white rounded" style="padding: 2px;" >Date Read</span>
            </label>
        </div>


        <fieldset class="p-2 rounded">

            <label for="rate" class="text-pink">
                <span class="bg-white rounded" style="padding: 2px;">Rate</span>
            </label>
            <div role="group" class="mt-2">
                <% for (let i = 1; i <= 10; i++) { %>
                    <input type="radio" class="btn-check btn-rate" name="rate" id="rate<%= i %>" value="<%= i %>" <%= i === book.rate ? "checked" : "" %>>
                    <label class="btn btn-outline-danger" for="rate<%= i %>"><%= i %></label>
                <% } %>
            </div>
        </fieldset>

         <div class="form-floating my-3">
            <textarea class="form-control input" id="summary" style="height: 100px" name="summary"><%= book.summary %></textarea>
            <label for="summary" class="text-pink">Write a short summary of the book</label>
        </div>

        <div class="w-100 d-flex justify-content-end ">
            <button type="submit" class="btn btn-success btn-green">Submit</button>
        </div>
        
    </form>

<%- include("partials/footer.ejs") %>